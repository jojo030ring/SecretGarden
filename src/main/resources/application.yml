spring:
  datasource:
    url: jdbc:mysql://localhost/secretgarden?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&useSSL=true&serverTimezone=UTC&tinyInt1isBit=false"
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true # >> system out?? ??? ??
        format_sql: true

    hibernate:
      ddl-auto: create

logging:
  level:
    org.hibernate.sql: debug # logger??? debug? ??

